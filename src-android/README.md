# Android wrapper scaffold

This folder is reserved for the Android wrapper generated by the Tauri mobile workflow (`tauri android init`).

It is committed now to lock in the transition path and avoid migration churn when mobile targets are enabled.
