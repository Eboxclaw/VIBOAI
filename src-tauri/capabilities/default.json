{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for desktop and mobile runtimes.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
